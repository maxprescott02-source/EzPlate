# UI/UX audit — 2 Sep 2026 (Phase 1, audit only)

Every screen rendered at 375, 390, 768, 1024, 1280, 1440 and 1920 px, in populated and
empty states, against the café's real data (the full catalogue, all plates and dishes,
the complete price history). 146 screen captures plus a modal, keyboard, scroll-bottom
and zero-results pass. **No code was changed in this phase.**

## Where the evidence lives — and why it is NOT in this repo

⚠️ **The capture set is deliberately not committed.** This repository is public, and the
populated screenshots show real plate costs, sell prices, food-cost percentages, supplier
names and a real phone number — exactly the class of business data CLAUDE.md records as
this repo's actual leak. A committed screenshot is world-readable forever.

- The full set (146 captures, sliced copies, modal shots, per-screen measurement JSON,
  and the harness that produced them) is preserved locally at
  `~/Documents/EzPlate-ui-audit-2026-09-02/`.
- It is **fully reproducible**: the harness boots the real app through the same Supabase
  shim the Playwright specs use, seeded from a read-only export. `node run.js` in that
  folder regenerates everything (`run.js` serves the repo with `python3 -m http.server`
  and uses the repo's own Playwright install).

## Setup

The app is online-only with sign-in mandatory, so production could not be pointed at
directly. Real data was exported read-only from Supabase and served through a boot shim
modelled on `tests/visual/_boot.js`, signed in as an owner. The empty state serves every
table empty; the signed-out and no-café gates were captured separately.

## Framework, styling, screens

- **Framework: none.** Hand-written static PWA — `index.html`, one browser script
  (`js/app.js`), `css/style.css`, `sw.js`. No build step, no router.
- **Styling:** one stylesheet, already substantially tokenised — light/dark palette on
  `:root` / `html[data-theme="dark"]`, a `--fs-*` type scale, a 4px `--sp-*` spacing
  scale, radius and motion tokens, plus an alias layer for legacy names. The debt is
  ~95 off-scale font-size literals (40× `13px`, 27× `12.5px`, 17× `11.5px`, 11× `13.5px`)
  and nine distinct breakpoint values (380, 520, 560, 639, 640, 700, 767, 768, 1023, 1024).
- **Screens** (tab panes toggled by `showTab`): Dashboard · Menu (`analysis`) · Plates
  library (`builder`) · Ingredients (`pantry`) · Products (`ingredients`) · Invoices ·
  Settings · Account · More (mobile hub, absorbs the last four below 1024) — plus the
  full-page plate builder (`#builderPage`), the signed-out and no-café gates, and ~20
  modal overlays. Navigation renders three ways: bottom tab bar <768, icon rail
  768–1023, full sidebar ≥1024.

## The verdict

Phones (375–390) and desktop (1024+) are in good shape — consistent header grid, no
overflow, big inputs, real empty states. **The 768–1023 tablet band is broken on all
three data tables**: the desktop column templates apply from `min-width:768` but their
fixed tracks only fit at ≥1024, so the Menu screen shows two-letter dish names, the
Ingredients screen grows 200px-tall rows, and the Products screen silently clips its
"Last change" column offscreen. One CSS commit per table fixes the worst of this audit.

## Defect table

Row ids (R1…R20) are the reference for Phase 2 commits.

| # | Screen | Breakpoint | Defect | Category | Severity | Proposed fix | Files | Functional risk |
|---|--------|-----------|--------|----------|----------|-------------|-------|------|
| R1 | Menu | 768–1023 | Name column ~50px; every dish name truncates to 2–4 letters ("Ba…", "Fet…") — dishes cannot be told apart | grid | **blocks use** | The desktop tracks `minmax(0,1fr) 90px 130px 100px 160px` apply from 768 but only fit at ≥1024. Band-specific column tracks (or keep the stacked mobile rows until 1024) | css/style.css :4575 | none — CSS only |
| R2 | Ingredients | 768–1023 | Name col ~90px; the "→ product" line wraps one word per line, rows reach 203px tall — unscannable | grid | **irritates** | Same treatment for `.king-band,.king-row` (`1fr 130px 110px 110px 100px`) at :4149 | css/style.css | none |
| R3 | Products | 768–1023 | "Last change" column + drift badges render past the right edge, clipped invisible — data silently hidden, no scroll, no hint | grid | **irritates** | `minmax(220px,1fr) 190px 110px 100px` sums wider than the container; band-specific tracks, or drop the column <1024 explicitly | css/style.css :4330 | none |
| R4 | Menu | 1024+ | Search field sits top-right at 200×40 while Plates/Products/Ingredients put it top-left at 400×40 (x1216 vs x256 at 1440) — the one list control that moves between pages | consistency | **irritates** | Move Menu's search into the same left slot and width | index.html + css/style.css | none — ids/handlers unchanged, markup order only |
| R5 | Builder page | ≤767 | Remove-line "×" is 15.5×36px; pack-price chips ("$…/kg ✎") are 27px tall — the row's main touch controls | touch | **irritates** | Hit-area padding + negative margin to 44px effective, visuals unchanged | css/style.css | none |
| R6 | Dashboard, links | ≤767 | Trend range buttons 32px tall; text links ~17px ("Privacy notice", "What is sent", wizard "Skip", phone link, sign-in links) | touch | **irritates** | `min-height:44px` on `.range-btn`; padding on the link classes | css/style.css | none |
| R7 | Menu, Products | ≤767 | Control-stack rhythm varies per screen (search lands at y142/y166/y196); secondary actions float alone right-aligned on their own orphan row ("Existing plate", "Import") | consistency | cosmetic | One shared control-row pattern: search + filter row, secondary action joins the filter row | css/style.css (+minor markup) | none |
| R8 | Empty states | all | Two primary CTAs at once: header "New plate" + empty-card "New plate" (same on Menu; empty Products shows four actions, two of them "New product") | consistency | cosmetic | Demote the header action while the first-run card shows — CSS `:has()` if reachable, else a 1-line JS class toggle → **needs approval** | css/style.css (poss. js/app.js) | low |
| R9 | Invoices dropzone | all | Helper text measures 4.04:1 (AA needs 4.5) — `--text-3` on the tinted panel. The stylesheet's own AA notes cover hover washes; this is a persistent surface | contrast | cosmetic | Use `--text-2` for `.invz-s` | css/style.css | none |
| R10 | Products | 1024 | Name and brand both ellipsise in one row ("…16'S 1-00… Pri…") — brand truncates to three letters | truncation | cosmetic | Brand min-width floor, or stack brand under name at this width | css/style.css | none |
| R11 | Edit modals | all | Destructive action in two places: Edit menu item centres "Delete item" in a strip below the footer; Edit ingredient puts "Remove" left in the footer | consistency | cosmetic | One pattern (left-in-footer) for both | index.html, css/style.css | none if markup-only |
| R12 | Menu zero-results | all | Plates' no-match state offers "Clear search & filters"; Menu's has no recovery link | states | cosmetic | Add the link to the Menu empty-result renderer — JS change → **needs approval** | js/app.js | needs approval |
| R13 | Toast | ≤767 | Toast (z90) lands on bottom-sheet content (covered the first Manage-menus row) and dropdown rows | stacking | cosmetic | Transient (~2s), low priority; CSS-only option is a higher toast offset while an overlay is open | css/style.css | low |
| R14 | All screens | 1920 | Content column left-anchored beside the sidebar; ~500px dead space right of content | layout | cosmetic | Centre the content wrap above ~1600px (`margin-inline:auto`) | css/style.css | none |
| R15 | Menu | ≤420 | The current menu's name is unreadable in the scope select (truncates after ~16 chars) while the % pill beside it has spare room | truncation | cosmetic | Let the select take the free width; the pill hugs its text | css/style.css | none |
| R16 | Dashboard dig-in | 768 | Dig-in card labels truncate despite free room in the card row | truncation | cosmetic | Rebalance the dig-grid columns at 768 | css/style.css | none |
| R17 | More screen | ≤1023 | More's title sits 11px higher than sibling titles (y73 vs y83.8); the "‹ More" back control is a 22px-wide target | consistency | cosmetic | Match `.scr-head` padding; widen the back hit area | css/style.css | none |
| R18 | List rows | 1024+ | Sibling tables disagree 3–4px on row height (Plates 41px, Menu 44–45px, Products 44px) | consistency | cosmetic | One row-height/padding token for desktop list rows | css/style.css | none |
| R19 | Install banner | 1024+ | Floating banner covers the right columns of tables until dismissed | stacking | cosmetic | Recommend **wontfix**: deliberately dismissible, its stacking was just rebuilt (v186), clearance margins cost more than they buy | — | — |
| R20 | Stylesheet | all | Token debt: ~95 font-size literals off the `--fs-*` scale; nine breakpoint values where three would do | tokens | cosmetic | Phase 2 item 1: value-preserving fold onto the existing scales (extend the scale where 12.5/13.5 are deliberate density values); rationalise breakpoints toward 640/768/1024. No visual change in that commit | css/style.css | none if value-preserving |
| R21 | Sidebar vs page header | 1024+ | The sidebar brand block and `.scr-head` are independent boxes: brand starts y16 h36 with its rule (`.side-brand-row` border-bottom) at **y69**; `.scr-head` starts y32 h48 with its rule at **y80** — the header line breaks 11px at the sidebar boundary | consistency / layout | **irritates** | One shared `--header-h` token consumed by both, identical vertical padding, both rules bottom-aligned so the hairline reads as one continuous line | css/style.css, index.html | none — CSS/markup only |
| R22 | Page header | all | The header **scrolls away** (measured: not covered — see note below); at scroll 400 `.scr-head` sits at −368/−335/−339 px and the sidebar stays, taking the page title and (on Dashboard/Menu) the scope identification with it | states | **blocks use** | `position:sticky; top:0` on `.scr-head` at `--header-h` with an explicit `background:var(--surface)`. Measured feasible: **no overflow-clipping ancestor** above `.scr-head` at any width. Layer at z ~50 — under nav (75), banner (78), toast (90). Mobile caveat: on Menu at ≤767 the current menu's name lives below the header (`.scr-sub` is desktop-only there), so the sticky header must show `.scr-sub` on mobile too or it pins a bare "Menu" | css/style.css (poss. index.html) | none if CSS-only; verify no z-index regression against R13/R19 |

### R22 — covered or scrolling? Measured: **scrolling.**

Probed at 1440, 768 and 390 (Menu screen, populated). `.scr-head` computes
`position:static`; after `scrollTo(0,400)` its top measures −368 / −335 / −339px, and
`elementFromPoint` at its old position finds ordinary row content (`span.mnu-id`,
`button.mnu-row`) — no overlay is ever on top of it. The sidebar stays because
`.bottomnav` is `position:fixed` (z75). So the fix is the sticky family, not a z-index
fix. Header geometry as measured (the R21 numbers):

| Box | 1440 | 768 | 390 |
|---|---|---|---|
| Sidebar brand block | top 16, h 36 (pad 6/6), rule y **69** (`.side-brand-row`) | rail is icon-only, no brand | — |
| `.scr-head` | top 32, h 48 (pad 0), rule y **80** | top 65, h 48, rule y 113 | top 61, h 69 (pad 12/12), rule y 130 |
| Mobile app bar | — | h 65, own border-bottom | h 61, own border-bottom |

Three header heights in one app (36-in-68, 48, 61/65/69) and two rules 11px apart at the
sidebar boundary. Sticky feasibility: **zero overflow-clipping ancestors** above
`.scr-head` at every width, so `position:sticky` against the document scroll works.

Notes:
- Header title/action alignment **screen-to-screen** measured consistent (h2 at
  x256/y32 on every desktop screen; header primaries all y38/h35). The sidebar-vs-page
  header mismatch is a separate axis — that is R21.
- The mobile/desktop radius split (10px/6px via `--radius-ctl`) and the ≤380px nav
  density tier are documented design decisions, not defects.
- The `--text-3` 4.04:1 contrast on *hover* surfaces is a recorded trade-off in the
  stylesheet; R9 is a different, persistent surface.

## What was checked and came back clean

- No horizontal page scroll in any of the 146 runs — every screen, width and state
- Every input ≥16px font — iOS will never zoom-on-focus
- Focus rings on every element sampled (26 `:focus-visible` rules, global + per-component)
- Tab order is logical: nav → sidebar group → content
- Safe-area insets handled (20 usages, `viewport-fit=cover`)
- Bottom stack behaves: content clears banner + tab bar at page bottom (the v186 work holds)
- Loading splash, offline toast, signed-out gate, no-café gate all present and legible
- Empty states are illustrated with real guidance; zero-search-results states exist
- Sticky cost panel and dropdown anchoring in the builder behave at every width
  (the v189 containing-block fix holds)
- Dark theme is fully tokenised (token review; screen-by-screen pass pending, see limits)

## Dark theme re-run — 3 Sep 2026: NO NEW ROWS

The full matrix was re-run with `data-theme="dark"` (the harness gained `--theme=dark`,
which seeds `cafeCost_theme` so the head script stamps the attribute before first paint):
all 146 captures, 0 boot failures. Findings:

- **Mechanical sweep: zero dark-only defects.** Contrast, clipped text, offscreen
  elements, horizontal scroll and small targets were diffed run-by-run against the light
  data. Layout facts are pixel-identical in every run; the only contrast failures in dark
  are the SAME six entries (by selector and text) the light run already records — the R9
  `.invz-s` class. Nothing fails in dark that does not fail in light.
- **Visual pass over 13 representative captures** (Dashboard/Menu/Products/Ingredients/
  builder/Invoices/Settings/More/Account across 390 and 1440, the empty Dashboard, and
  both gates): tokens hold everywhere sampled — charts, pills, empty-state illustrations,
  the theme segment control and the install banner all render correctly.
- **Honest scope:** 13 of 146 captures were eyeballed; the other 133 are covered by the
  mechanical sweep only, which cannot judge aesthetics (it can judge contrast, overflow
  and geometry, and did). Modals were not re-captured in dark.

## Limits of this audit

Rendered in headless Chromium on macOS, light theme (dark theme re-run 3 Sep 2026 — see
above). Not exercised: real iOS Safari
(`docs/PHONE.md` covers device checks), the invoice *review* table (needs a
parsed invoice; its renderer has its own regression suite), and print stylesheets.
Contrast figures are computed from flattened backgrounds and worst-case sampled, not
exhaustive.

## Phase 2 plan (constraints: no functional change, no new dependencies, CSS/markup only, one defect class per commit)

1. **Token layer only** — fold off-scale literals onto the existing `--fs-*`/`--sp-*`
   scales and rationalise breakpoints, value-preserving, zero visual change. (The scales
   already exist; this is completion, not creation.)
2. **Shared layout primitive** — `.scr-head` already locks title + action; extend it to
   lock the search/filter row too, and bring the Menu screen onto it (R4, R7).
3. **One commit per defect class, highest severity first**: R22 (sticky header) →
   R1 → R2 → R3 (the tablet band), then R21 (header unification — natural companion to
   R22 since both consume `--header-h`), then R5/R6 (touch targets), then the cosmetic
   set. The R22 commit must re-run the layer checks against the toast (z90) and install
   banner (z78) and show scrolled before/after shots at 390 and 1440. Every commit: suite +
   smoke + Playwright green, before/after shots at 390 and 1440, diff confined to
   `css/style.css`/`index.html` or the exception named.

R8 and R12 need a yes for one-line JS changes; everything else is CSS/markup within the
constraints. Done means every row above is marked fixed, deferred or wontfix with a reason.

## Phase 2 progress

| Rows | Status |
|---|---|
| R21 + R22 | **Fixed, `ezplate-v190`** (batch 231). One `--header-h` token; both hairlines end at y80 and the page-side rule reaches the sidebar edge (Max's R21 instruction, partially reversing his 12 Aug matching-edges call — the rule only; content edges unchanged). `.scr-head` sticky (`.bld-head` excluded — the builder's own `.bld-sum` pins at top:24 and its mobile header is three rows tall). On a phone the Menu header now shows the current menu's name so the pinned bar identifies its scope. Pinned via `env(safe-area-inset-top)`; `docs/PHONE.md` has the standalone-notch check. `tests/visual/v190-sticky-header.spec.js` pins all of it; `v155-shell-edges.spec.js` retitled to the surviving half of the matching-edges contract. |
| Dark re-run | Done 3 Sep 2026, **no new rows** — see the dark-theme section above. |
