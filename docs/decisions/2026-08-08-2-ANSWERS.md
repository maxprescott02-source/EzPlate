# Answers — decisions #2 (asked 8 Aug 2026, answered 9 Aug 2026)

Max's answers to `2026-08-08-2.html`, pasted 9 Aug 2026. All five took the recommendation.

## 1. Drop the empty `kitchen_items` table — **Yes, drop it**
Applied the same day: re-verified 0 rows and no code reference, dropped over the production MCP
(staging unavailable, said out loud), recorded in `supabase/migrations/20260809_drop_kitchen_items.sql`
with the one-statement rollback. The queue item is done.

## 2. Dish with a lost recipe link — **Heal when one match, ask when several**
The `ensurePlateForDish` item is UNBLOCKED and now carries its decision: when an unlinked dish's name
matches exactly one library plate, relink it; when several match, ask; production has 0 such rows
today, so this builds in a future batch at normal priority.

## 3. Stale `CLAUDE.md` sentences — **Yes, fix all three**
Applied the same day — and read as covering the two same-class additions the v125 audit found
(the "plates persist {kid,qty} ONLY" line and Tier 3 presenting never-loaded staging as an available
safeguard), which were listed in the queue item when Max answered. **Five lines corrected in total;
if the wider scope overreads the yes, say so and lines 4–5 revert.** None changes what the code does.

## 4. Insight rule D — **Close it, leave the rule at the code**
The queue item is closed. The rule stays as the comment at the function it governs
(`deriveInsights`), which is where CLAUDE.md's own "true but inferable" test says it belongs.

## 5. Mutation testing (Stryker, dev-only) — **Yes, dev-only**
The queue item is UNBLOCKED and becomes a normal `next` item: add the dev-dependency, score the
fragile areas specifically, kill or document every surviving mutant there. Nothing ships to the
client. (The audit counted six "test that cannot fail" incidents — this is the standing fix.)
