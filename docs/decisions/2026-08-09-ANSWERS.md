# Answers — decisions of 9 Aug 2026 (`2026-08-09.html`)

Answered by Max, 9 Aug 2026, in chat: **all three took the recommendation.**
(The HTML file's interactive bits did not work well on his device — the answers came verbally.
Next decision file: plain readable text first, buttons second; do not depend on the copy button.)

1. **Dashboard "vs last month" change pill: A — stays out.**
   The v98 deletion stands, now twice-decided (v98 on real data, reaffirmed today).
   The chart below the number is the one trend surface. The queue item is closed as decided-no,
   not built. Do not re-propose without new evidence.

2. **The empty-restore test (restore step 3): A — before the multi-tenant work starts.**
   When the v3 redesign phase finishes, prepare everything (fresh export first) and ask for
   Max's go on the day. The queue item stays `blocked` until that day — the block is now a
   scheduled handoff, not an open question.

3. **Staff roles: B — owner + working staff, two roles.**
   Staff can import invoices and edit ingredients/plates. Staff cannot delete plates or menus,
   change the target, restore backups, or touch billing. No manager role unless a real person
   at a real café needs one later. This is the product answer everything technical in the
   multi-tenant phase builds against; recorded on the Roles queue item.
