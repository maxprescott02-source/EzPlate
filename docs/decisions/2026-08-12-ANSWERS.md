# Answers - 12 Aug 2026

Max's replies to `2026-08-12.md` (§1, §2) and `2026-08-12-2.md` (§3, §4), given 12 Aug 2026.
Recorded verbatim, because a decision that has to be made twice was not recorded properly.

---

**1. Where the second button goes on a phone** — *"a and yes it should be"*

**Option A**: the second action moves into the existing `.plib-controls` row directly beneath the header, mobile only (≤767).
Desktop headers keep both, unchanged.
**And 360px becomes a supported width**, added to the three named specs alongside 380 and 430.

Recorded in `docs/QUEUE.md` item 1, which is now `next` rather than `blocked`.
The proposal in that item is now its specification, not a candidate.

---

**2. The restore full-wipe test** — *"yes you can do it no one currently using the software"*

The go is given, and the second clause is the operating window: nothing else is writing.
The preconditions are unchanged and are not waived by the go - a fresh export taken minutes before, the one-statement rollback written down, the real file rehearsed against staging first.

Recorded in `docs/QUEUE.md` item 11, now `next`.

---

**3. The staging warning deleted from `CLAUDE.md`** — *"yes leave it"*

Confirmed with the option of reinstating the old caution, and declined.
So the removal of "defer destructive ones" from that bullet is agreed: it was a weaker duplicate of the standing destructive-work rule, not a second protection.

Recorded at the site in `CLAUDE.md`, Tier 3, with "do not restore it".

---

**4. The CSS-syntax rule** — *"add it"*

Added to `CLAUDE.md` Tier 1 as **"A CSS syntax error is SILENT, and it discards every rule after it"**, carrying the diagnostic order it exists to fix: check the rule EXISTS before reasoning about why it loses.

This closes the thread AUDIT-v156 D2 flagged as heading for the same fate as the four corrections that were written in a handover and reached nowhere.
